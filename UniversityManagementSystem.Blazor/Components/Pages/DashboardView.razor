@code {
    [Parameter]
    public int TotalStudents { get; set; }

    [Parameter]
    public int FacultyCount { get; set; }

    [Parameter]
    public int CoursesCount { get; set; }

    [Parameter]
    public decimal TotalFees { get; set; }

    [Parameter]
    public List<StudentDto>? RecentStudents { get; set; }

    [Parameter]
    public List<DepartmentDto>? Departments { get; set; }

    [Parameter]
    public EventCallback<string> OnEditStudent { get; set; }

    [Parameter]
    public EventCallback<string> OnDeleteStudent { get; set; }
}

<div class="page-content">
    <div class="page-header">
        <h2>لوحة التحكم</h2>
        <button class="btn btn-primary ripple-effect click-effect">
            <i class="fas fa-plus"></i> إضافة جديد
        </button>
    </div>

    <!-- بطاقات الإحصائيات -->
    <div class="stats-cards">
        <!-- بطاقات الإحصائيات -->
    </div>

    <!-- جدول الطلاب -->
    <div class="table-container">
        <h3>أحدث الطلاب المسجلين</h3>
        <table>
            <!-- محتوى الجدول -->
        </table>
    </div>
</div>