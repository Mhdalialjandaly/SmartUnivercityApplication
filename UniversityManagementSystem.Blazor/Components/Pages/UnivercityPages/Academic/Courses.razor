@page "/academic/courses"
@attribute [Authorize]
@inject ICourseService CourseService
@inject IDepartmentService DepartmentService
@rendermode InteractiveServer

<PageTitle>إدارة المواد الدراسية - الجامعة الذكية</PageTitle>

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1 class="h3 mb-0 fw-bold text-primary">إدارة المواد الدراسية</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/">الرئيسية</a></li>
                            <li class="breadcrumb-item"><a href="/academic">الشؤون الأكاديمية</a></li>
                            <li class="breadcrumb-item active">المواد الدراسية</li>
                        </ol>
                    </nav>
                </div>
                <div>
                    <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addCourseModal">
                        <i class="fas fa-plus me-2"></i>إضافة مادة جديدة
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- إحصائيات سريعة -->
    <div class="row g-4 mb-4">
        <div class="col-md-3">
            <div class="card border-primary border-start border-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="text-muted">إجمالي المواد</h5>
                            <h2 class="mb-0 fw-bold">@courseStatistics.TotalCourses</h2>
                        </div>
                        <div class="bg-primary bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-book fa-2x text-primary"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-success border-start border-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="text-muted">النشطة</h5>
                            <h2 class="mb-0 fw-bold">@courseStatistics.ActiveCourses</h2>
                        </div>
                        <div class="bg-success bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-check-circle fa-2x text-success"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-warning border-start border-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="text-muted">الطلاب المسجلون</h5>
                            <h2 class="mb-0 fw-bold">@courseStatistics.TotalStudents</h2>
                        </div>
                        <div class="bg-warning bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-users fa-2x text-warning"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-info border-start border-4">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="text-muted">الإيرادات</h5>
                            <h2 class="mb-0 fw-bold">@courseStatistics.TotalRevenue.ToString("C")</h2>
                        </div>
                        <div class="bg-info bg-opacity-10 p-3 rounded-circle">
                            <i class="fas fa-money-bill-wave fa-2x text-info"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- قائمة المواد -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-white">
                    <div class="row align-items-center">
                        <div class="col-md-6">
                            <h5 class="mb-0">قائمة المواد الدراسية</h5>
                        </div>
                        <div class="col-md-6">
                            <div class="d-flex justify-content-md-end gap-2">
                                <div class="input-group" style="max-width: 300px;">
                                    <span class="input-group-text"><i class="fas fa-search"></i></span>
                                    <input type="text" class="form-control" placeholder="بحث عن مادة..." @bind="searchTerm" @oninput="SearchCourses">
                                </div>
                                <select class="form-select" style="max-width: 150px;" @onchange="FilterByDepartment">
                                    <option value="">جميع الأقسام</option>
                                    @foreach (var dept in departments)
                                    {
                                        <option value="@dept.Id">@dept.Name</option>
                                    }
                                </select>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>الرمز</th>
                                    <th>اسم المادة</th>
                                    <th>القسم</th>
                                    <th>المحاضر</th>
                                    <th>الساعات</th>
                                    <th>الكلفة</th>
                                    <th>الطلاب</th>
                                    <th>الإيرادات</th>
                                    <th>الحالة</th>
                                    <th>الإجراءات</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (courses.Any())
                                {
                                    @for (int i = 0; i < courses.Count; i++)
                                    {
                                        var course = courses[i];
                                        <tr>
                                            <td><strong>@course.Code</strong></td>
                                            <td>@course.Name</td>
                                            <td>@course.DepartmentName</td>
                                            <td>@course.Instructor</td>
                                            <td class="text-center">@course.Credits</td>
                                            <td>@course.Fee.ToString("C")</td>
                                            <td class="text-center">@course.Fee</td>
                                            <td>@course.Fee.ToString("C")</td>
                                            <td>
                                                <span class="badge @(course.IsActive ? "bg-success" : "bg-secondary")">
                                                    @(course.IsActive ? "نشط" : "غير نشط")
                                                </span>
                                            </td>
                                            <td>
                                                <div class="dropdown">
                                                    <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                                        <i class="fas fa-ellipsis-v"></i>
                                                    </button>
                                                    <ul class="dropdown-menu">
                                                        <li><a class="dropdown-item" href="/academic/courses/@course.Id"><i class="fas fa-eye me-2"></i>عرض التفاصيل</a></li>
                                                        <li><a class="dropdown-item" href="/academic/courses/@course.Id/lectures"><i class="fas fa-chalkboard-teacher me-2"></i>المحاضرات</a></li>
                                                        <li><a class="dropdown-item" href="/academic/courses/@course.Id/students"><i class="fas fa-users me-2"></i>الطلاب المسجلون</a></li>
                                                        <li><hr class="dropdown-divider"></li>
                                                        <li><a class="dropdown-item" href="javascript:void(0)" @onclick="() => EditCourse(course)"><i class="fas fa-edit me-2"></i>تعديل</a></li>
                                                        <li><a class="dropdown-item text-danger" href="javascript:void(0)" @onclick="() => DeleteCourse(course.Id)"><i class="fas fa-trash me-2"></i>حذف</a></li>
                                                    </ul>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="10" class="text-center">لا توجد مواد دراسية</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    <!-- ترقيم الصفحات -->
                    @if (totalPages > 1)
                    {
                        <nav aria-label="Page navigation">
                            <ul class="pagination justify-content-center">
                                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                                    <a class="page-link" href="javascript:void(0)" @onclick="() => GoToPage(currentPage - 1)" tabindex="-1">السابق</a>
                                </li>

                                @for (int i = 1; i <= totalPages; i++)
                                {
                                    <li class="page-item @(i == currentPage ? "active" : "")">
                                        <a class="page-link" href="javascript:void(0)" @onclick="() => GoToPage(i)">@i</a>
                                    </li>
                                }

                                <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                                    <a class="page-link" href="javascript:void(0)" @onclick="() => GoToPage(currentPage + 1)">التالي</a>
                                </li>
                            </ul>
                        </nav>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal إضافة مادة جديدة -->
<div class="modal fade" id="addCourseModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">إضافة مادة دراسية جديدة</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">رمز المادة *</label>
                            <input type="text" class="form-control" placeholder="أدخل رمز المادة" @bind="newCourse.Code" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">اسم المادة *</label>
                            <input type="text" class="form-control" placeholder="أدخل اسم المادة" @bind="newCourse.Name" required>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label class="form-label">القسم *</label>
                            <select class="form-select" @bind="newCourse.DepartmentId" required>
                                <option value="">اختر القسم</option>
                                @foreach (var dept in departments)
                                {
                                    <option value="@dept.Id">@dept.Name</option>
                                }
                            </select>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">المحاضر *</label>
                            <input type="text" class="form-control" placeholder="أدخل اسم المحاضر" @bind="newCourse.Instructor" required>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">الساعات *</label>
                            <input type="number" class="form-control" placeholder="أدخل عدد الساعات" @bind="newCourse.Credits" required>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label class="form-label">الكلفة *</label>
                            <input type="number" step="0.01" class="form-control" placeholder="أدخل الكلفة" @bind="newCourse.Fee" required>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">الحد الأقصى للطلاب</label>
                            <input type="number" class="form-control" placeholder="أدخل الحد الأقصى" @bind="newCourse.MaxStudents">
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">الفصل الدراسي</label>
                            <select class="form-select" @bind="newCourse.Semester">
                                <option value="1">الفصل الأول</option>
                                <option value="2">الفصل الثاني</option>
                            </select>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">نوع المادة</label>
                            <select class="form-select" @bind="newCourse.CourseType">
                                <option value="نظري">نظري</option>
                                <option value="عملي">عملي</option>
                                <option value="نظري وعملي">نظري وعملي</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">الحالة</label>
                            <select class="form-select" @bind="newCourse.IsActive">
                                <option value="true">نشط</option>
                                <option value="false">غير نشط</option>
                            </select>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">المتطلبات السابقة</label>
                        <input type="text" class="form-control" placeholder="أدخل رموز المواد المتطلبة مفصولة بفواصل" @bind="newCourse.Prerequisites">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">الوصف</label>
                        <textarea class="form-control" rows="3" placeholder="أدخل وصف المادة" @bind="newCourse.Description"></textarea>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
                <button type="button" class="btn btn-primary" @onclick="CreateCourse">حفظ المادة</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal تعديل المادة -->
<div class="modal fade" id="editCourseModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">تعديل المادة الدراسية</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                @if (editingCourse != null)
                {
                    <form>
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label">رمز المادة *</label>
                                <input type="text" class="form-control" placeholder="أدخل رمز المادة" @bind="editingCourse.Code" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label">اسم المادة *</label>
                                <input type="text" class="form-control" placeholder="أدخل اسم المادة" @bind="editingCourse.Name" required>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="form-label">القسم *</label>
                                <select class="form-select" @bind="editingCourse.DepartmentId" required>
                                    <option value="">اختر القسم</option>
                                    @foreach (var dept in departments)
                                    {
                                        <option value="@dept.Id">@dept.Name</option>
                                    }
                                </select>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">المحاضر *</label>
                                <input type="text" class="form-control" placeholder="أدخل اسم المحاضر" @bind="editingCourse.Instructor" required>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">الساعات *</label>
                                <input type="number" class="form-control" placeholder="أدخل عدد الساعات" @bind="editingCourse.Credits" required>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <label class="form-label">الكلفة *</label>
                                <input type="number" step="0.01" class="form-control" placeholder="أدخل الكلفة" @bind="editingCourse.Fee" required>
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">الحد الأقصى للطلاب</label>
                                <input type="number" class="form-control" placeholder="أدخل الحد الأقصى" @bind="editingCourse.MaxStudents">
                            </div>
                            <div class="col-md-4 mb-3">
                                <label class="form-label">الفصل الدراسي</label>
                                <select class="form-select" @bind="editingCourse.Semester">
                                    <option value="1">الفصل الأول</option>
                                    <option value="2">الفصل الثاني</option>
                                </select>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label">نوع المادة</label>
                                <select class="form-select" @bind="editingCourse.CourseType">
                                    <option value="نظري">نظري</option>
                                    <option value="عملي">عملي</option>
                                    <option value="نظري وعملي">نظري وعملي</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label">الحالة</label>
                                <select class="form-select" @bind="editingCourse.IsActive">
                                    <option value="true">نشط</option>
                                    <option value="false">غير نشط</option>
                                </select>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">المتطلبات السابقة</label>
                            <input type="text" class="form-control" placeholder="أدخل رموز المواد المتطلبة مفصولة بفواصل" @bind="editingCourse.Prerequisites">
                        </div>

                        <div class="mb-3">
                            <label class="form-label">الوصف</label>
                            <textarea class="form-control" rows="3" placeholder="أدخل وصف المادة" @bind="editingCourse.Description"></textarea>
                        </div>
                    </form>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
                <button type="button" class="btn btn-primary" @onclick="UpdateCourse">حفظ التغييرات</button>
            </div>
        </div>
    </div>
</div>

@code {
    private List<CourseDto> courses = new();
    private List<DepartmentDto> departments = new();
    private CourseDto newCourse = new();
    private CourseDto editingCourse;
    private CourseStatisticsDto courseStatistics = new();
    private string searchTerm ;
    private int selectedDepartmentId;

    // ترقيم الصفحات
    private int currentPage = 1;
    private int totalPages = 1;
    private int pageSize = 10;

    protected override async Task OnInitializedAsync()
    {
        await LoadDepartments();
        await LoadCourseStatistics();
        await LoadCourses();
    }

    private async Task LoadDepartments()
    {
        try
        {
            departments = await DepartmentService.GetAllDepartmentsAsync();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading departments: {ex.Message}");
        }
    }

    private async Task LoadCourses()
    {
        try
        {
            var result = await CourseService.GetCoursesAsync(
                currentPage,
                pageSize,
                searchTerm,
                selectedDepartmentId,
                true); 

            courses = result.Courses;
            totalPages = result.TotalPages;
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading courses: {ex.Message}");
        }
    }

    private async Task LoadCourseStatistics()
    {
        try
        {
            courseStatistics = await CourseService.GetCourseStatisticsAsync();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading course statistics: {ex.Message}");
        }
    }

    private async Task SearchCourses(ChangeEventArgs e)
    {
        searchTerm = e.Value?.ToString() ?? "";
        currentPage = 1;
        await LoadCourses();
    }

    private async Task FilterByDepartment(ChangeEventArgs e)
    {
        var value = e.Value?.ToString();
        selectedDepartmentId = string.IsNullOrEmpty(value) ? 0 : int.Parse(value);
        currentPage = 1;
        await LoadCourses();
    }

    private async Task GoToPage(int page)
    {
        if (page >= 1 && page <= totalPages)
        {
            currentPage = page;
            await LoadCourses();
        }
    }

    private async Task CreateCourse()
    {
        try
        {
            newCourse.CreatedDate = DateTime.Now;
            newCourse.AcademicYear = DateTime.Now.Year;

            var createdCourse = await CourseService.CreateCourseAsync(newCourse);

            // إعادة تعيين النموذج
            newCourse = new CourseDto();

            // تحديث القائمة والإحصائيات
            await LoadCourseStatistics();
            await LoadCourses();

            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error creating course: {ex.Message}");
        }
    }

    private void EditCourse(CourseDto course)
    {
        editingCourse = new CourseDto
            {
                Id = course.Id,
                Code = course.Code,
                Name = course.Name,
                DepartmentId = course.DepartmentId,
                Instructor = course.Instructor,
                Credits = course.Credits,
                Fee = course.Fee,
                MaxStudents = course.MaxStudents,
                CurrentStudents = course.CurrentStudents,
                IsActive = course.IsActive,
                Description = course.Description,
                Semester = course.Semester,
                AcademicYear = course.AcademicYear,
                Prerequisites = course.Prerequisites,
                CourseType = course.CourseType
            };

        // فتح الـ Modal
        // JS.InvokeVoidAsync("bootstrap.Modal.getOrCreateInstance", "#editCourseModal")?.show();
    }

    private async Task UpdateCourse()
    {
        if (editingCourse != null)
        {
            try
            {
                await CourseService.UpdateCourseAsync(editingCourse.Id, editingCourse);

                // إغلاق الـ Modal
                // JS.InvokeVoidAsync("bootstrap.Modal.getInstance", "#editCourseModal")?.hide();

                // تحديث القائمة والإحصائيات
                await LoadCourseStatistics();
                await LoadCourses();

                StateHasChanged();
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Error updating course: {ex.Message}");
            }
        }
    }

    private async Task DeleteCourse(int courseId)
    {
        try
        {
            await CourseService.DeleteCourseAsync(courseId);

            // تحديث القائمة والإحصائيات
            await LoadCourseStatistics();
            await LoadCourses();

            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error deleting course: {ex.Message}");
        }
    }
}